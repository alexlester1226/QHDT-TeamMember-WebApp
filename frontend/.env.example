# Copy this file to `.env` and adjust for your environment.
# Create React App exposes any variable prefixed with REACT_APP_ at build time.
# The trailing slash matters — `api.js` treats this value as the axios baseURL,
# and request paths are appended without a leading slash duplicating it.

REACT_APP_API_URL=http://127.0.0.1:8000/api/
